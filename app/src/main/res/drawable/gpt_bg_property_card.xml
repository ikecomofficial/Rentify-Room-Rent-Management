<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background white -->
    <solid android:color="@color/bg_list_item" />

    <!-- Rounded corners -->
    <corners android:radius="12dp" />

    <!-- Shiny stroke -->
    <stroke
        android:width="0.4dp"
        android:color="@color/purple_700" /> <!-- 50% transparent light teal -->

</shape>
