<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/bg_login_google_btn" />
    <corners android:radius="10dp" />

    <!-- Shiny stroke -->
    <stroke
        android:width="0.3dp"
        android:color="@color/purple_700" /> <!-- 50% transparent light teal -->
</shape>
